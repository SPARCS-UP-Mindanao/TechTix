s3_upload_object:
  handler: event_s3_uploads.handler
  environment:
    STAGE: ${self:provider.stage}
  layers:
    - { Ref: PythonRequirementsLambdaLayer }
  events:
    - s3: 
        bucket: sparcsBucket
        event: s3:ObjectCreated:*
        rules:
          - prefix: events/
